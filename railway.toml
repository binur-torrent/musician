[build]
builder = "dockerfile"
dockerfilePath = "apps/web/Dockerfile"

[deploy]
startCommand = "pnpm --filter @musician/web start -- --hostname 0.0.0.0"
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
numReplicas = 1
